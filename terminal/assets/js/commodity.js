$(document).ready(function() {
	// hides the slickbox as soon as the DOM is ready
	// (a little sooner than page load)
	$('#hiddenbox0').hide();
	// toggles the slickbox on clicking the noted link
	$('a#jqtextcall0').click(function() {
		$('#hiddenbox0').slideToggle(400);
		return false;
	});
});
$(document).ready(function() {
	// hides the slickbox as soon as the DOM is ready
	// (a little sooner than page load)
	$('#hiddenboxc1').hide();
	// toggles the slickbox on clicking the noted link
	$('a#jqtextcallc1').click(function() {
		$('#hiddenboxc1').slideToggle(400);
		return false;
	});
});

/*改begin*/

$(document).ready(function() {
	// hides the slickbox as soon as the DOM is ready
	// (a little sooner than page load)
	$('#hiddenboxc2').hide();
	// toggles the slickbox on clicking the noted link
	$('a#jqtextcallc2').click(function() {
		$('#hiddenboxc2').slideToggle(400);
		return false;
	});
});

/*改end*/